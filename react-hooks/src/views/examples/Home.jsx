import React from 'react';
import PageTitle from '../../components/layout/PageTitle';

const Home = (props) => (
  <div className="Home">
    <PageTitle
      title="Hooks"
      subtitle="Hooks are functions that let you “hook into” React state and lifecycle features from function components."
    />
  </div>
);

export default Home;
